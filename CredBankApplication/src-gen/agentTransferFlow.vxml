<?xml version="1.0" encoding="utf-8"?>
<vxml version="2.1" xml:lang="en-US"
		xmlns="http://www.w3.org/2001/vxml"
		xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
		xmlns:gvp="http://www.genesyslab.com/2006/vxml21-extension" application="../src/ComposerRoot.vxml">

	
	<!-- VXML code generated by Composer 8.1.561.24 -->
	<!-- Diagram created by Composer: Composer 8.1.561.24 -->
	<!-- Project version: 8.1.561.24 -->
	<!-- Diagram version: 1.0 CreatedOn:  -->
	<!-- Application Author:  -->
	<!-- Release Mode: false -->

	<!-- Global Scripts includes -->
	<script src="../include/common.js" />
	<script src="../include/ctic.js" />
	<script src="../include/json.js" />
	<script src="../include/DataAccess.js" />

	<!-- Global Vars declarations -->
	<script>
		var AppState = new Object();
		AppState.System = new Object();
		AppState.System.RetainInputTypeVariableDefaultValue = false;
		AppState.CallUUID = session.connection.uuid;
		AppState.ANI = getANI();
		AppState.DNIS = getDNIS();
		AppState.USE_LCASE_USERDATAKEY = initSystemTypeVariable('USE_LCASE_USERDATAKEY',0);
  		
  		if (session.connection.protocol.sip.headers != undefined &amp;&amp;
			    session.connection.protocol.sip.headers['x-genesys-gvp-session-id']){
			AppState.GVPSessionID = session.connection.protocol.sip.headers['x-genesys-gvp-session-id'];
			AppState.g_CTICCall = isCTICCall(AppState.GVPSessionID);
		} else {
			AppState.GVPSessionID = 'undefined';
			AppState.g_CTICCall = 'false';
		}
  			
		
		AppState.preferLanguage = '';
		AppState.APP_ASR_LANGUAGE = initSystemTypeVariable('APP_ASR_LANGUAGE','en-US');
		AppState.APP_LANGUAGE = initSystemTypeVariable('APP_LANGUAGE','en-US');
		AppState.COMPOSER_WSSTUBBING = initSystemTypeVariable('COMPOSER_WSSTUBBING','0');
		AppState.EnableFCR = initSystemTypeVariable('EnableFCR',true);
		AppState.EnableReports = initSystemTypeVariable('EnableReports',false);
		AppState.EnableSNMP = initSystemTypeVariable('EnableSNMP',false);
		AppState.GRAMMARFILEDIR = initSystemTypeVariable('GRAMMARFILEDIR','../Resources/Grammars');
		AppState.LAST_EVENT_ELEMENT = initSystemTypeVariable('LAST_EVENT_ELEMENT','undefined');
		AppState.LAST_EVENT_LINE = initSystemTypeVariable('LAST_EVENT_LINE','undefined');
		AppState.LAST_EVENT_MSG = initSystemTypeVariable('LAST_EVENT_MSG','undefined');
		AppState.LAST_EVENT_NAME = initSystemTypeVariable('LAST_EVENT_NAME','undefined');
		AppState.LAST_EVENT_URL = initSystemTypeVariable('LAST_EVENT_URL','undefined');
		AppState.OCS_Record = initSystemTypeVariable('OCS_Record',getCallflowOCSRecord());
		AppState.OCS_RecordURI = initSystemTypeVariable('OCS_RecordURI',getCallflowRecordURI());
		AppState.OCS_URI = initSystemTypeVariable('OCS_URI',getCallflowOCSURI());
		AppState.PREV_APP_ASR_LANGUAGE = initSystemTypeVariable('PREV_APP_ASR_LANGUAGE','undefined');
		AppState.PREV_APP_LANGUAGE = initSystemTypeVariable('PREV_APP_LANGUAGE','undefined');
		AppState.SCRIPTSDIR = initSystemTypeVariable('SCRIPTSDIR','../Scripts');
		AppState.VOXFILEDIR = initSystemTypeVariable('VOXFILEDIR','../Resources/Prompts');
		AppState.ONEFA = '';
		AppState.AgentTrans = undefined;
	</script>

		
	<!-- Global Scripts -->
	<script srcexpr="AppState.VOXFILEDIR + '/PromptSwitch.js'" />
	<script srcexpr="AppState.VOXFILEDIR + '/en-US/en-US.js'" />
	<script>var f = new Format();</script>

	
	<script srcexpr="AppState.VOXFILEDIR + '/en-US/customprompts.js'" />
	
	
	<!-- Global Properties declarations -->
	<property name="com.genesyslab.logtoasr" value="false" />   	
	<property name="com.genesyslab.loglevel" value="5" />

	<!-- Metadata information -->
	<meta name="com.genesyslab.studio.resourcePath" content="/Callflows/agentTransferFlow.callflow"/>

	<!-- Global links -->
	<form>	
		<!-- VAR Parent Action Param -->
		<var name="varActionParentId" expr="''"/>
		<var name="appSession" expr="''"/>
		<var name="errorName" expr="''"/>
		<var name="TRANSFERORDISCONNECT" expr="''"/>
		<block name="paramAssignment">
			<assign name="AppState.appSession" expr="appSession"/>
			<assign name="AppState.errorName" expr="errorName"/>
			<assign name="AppState.TRANSFERORDISCONNECT" expr="TRANSFERORDISCONNECT"/>
		</block>
		<block name="varAction">
			<if cond="varActionParentId==''">
				<log label="com.genesyslab.var.ActionStart" gvp:cond="AppState.EnableReports">agentTransferFlow</log>
			<else/>
				<log label="com.genesyslab.var.ActionStart" gvp:cond="AppState.EnableReports">agentTransferFlow|parentID=<value expr="varActionParentId"/></log>
			</if>
		</block>
		<block>
			<log expr="'Running agentTransferFlow workflow.'"/>
			<log expr="'Code Generated by Composer: 8.1.561.24.'"/>
			<log expr="'Diagram created/upgraded by Composer: Composer 8.1.561.24.'"/>
			<log expr="'Project version: 8.1.561.24.'"/>
			<log expr="'Diagram version: 1.0 CreatedOn: .'"/>
			<goto next="#angentTransferEntry" />
		</block>
	</form>
	<!-- This is the Entry block -->
	<form id="angentTransferEntry">
		<block name="Entry">
			<goto next="#agenTransferconfigProperty" />
		</block>
	</form>	
	
	<catch event="connection.disconnect.hangup">
		<assign name="AppState.LAST_EVENT_ELEMENT" expr="_element"/>
    	<assign name="AppState.LAST_EVENT_MSG" expr="_message"/>
    	<assign name="AppState.LAST_EVENT_NAME" expr="_event"/>
    	<assign name="AppState.LAST_EVENT_LINE" expr="_line"/>
    	<assign name="AppState.LAST_EVENT_URL" expr="_url"/>
		<script>storeCallflowException(_element, _message, _event, _line, _url)</script>  
			<goto next="#Hangup_ASSIGN" />
    </catch>
	<catch event="error">
		<assign name="AppState.LAST_EVENT_ELEMENT" expr="_element"/>
    	<assign name="AppState.LAST_EVENT_MSG" expr="_message"/>
    	<assign name="AppState.LAST_EVENT_NAME" expr="_event"/>
    	<assign name="AppState.LAST_EVENT_LINE" expr="_line"/>
    	<assign name="AppState.LAST_EVENT_URL" expr="_url"/>
		<script>storeCallflowException(_element, _message, _event, _line, _url)</script>  
			<goto next="#error_ASSIGN" />
    </catch>
	<catch event=".">
		<assign name="AppState.LAST_EVENT_ELEMENT" expr="_element"/>
    	<assign name="AppState.LAST_EVENT_MSG" expr="_message"/>
    	<assign name="AppState.LAST_EVENT_NAME" expr="_event"/>
    	<assign name="AppState.LAST_EVENT_LINE" expr="_line"/>
    	<assign name="AppState.LAST_EVENT_URL" expr="_url"/>
		<script>storeCallflowException(_element, _message, _event, _line, _url)</script>  
			<goto next="#allError_ASSIGN" />
    </catch>    
    

	<!-- This is the Backend logic block -->
	<form id="agenTransferconfigProperty">

		<var name="AppStateString" expr="JSON.stringify(AppState)" />	
		<block>
			<data name="agenTransferconfigPropertyData" src="../src/agentTransferConfig.jsp" method="post"  enctype="multipart/form-data"
				namelist="AppStateString" />
        	<if cond="typeof agenTransferconfigPropertyData.errorMsg == 'undefined'">
        		<assign name="AppState.agenTransferconfigPropertyResult" expr="agenTransferconfigPropertyData" />
            	<if cond="typeof AppState.agenTransferconfigPropertyResult.appSession != 'undefined'">
            		<assign name="AppState.appSession" expr="AppState.agenTransferconfigPropertyResult.appSession" />
            	</if>
				<goto next="#agentCoonectBranch" />
        	<else/>
        		<throw event="error.com.genesyslab.composer.servererror" messageexpr="agenTransferconfigPropertyData.errorMsg" />
        	</if>
		</block>
	</form>

	<!-- This is the Prompt block -->
	<form id="agentTransfer">
        
    	<block name="agentTransferP0">
  			<prompt bargein="true"  gvp:langexpr="AppState.APP_LANGUAGE">
				<audio expr="getAudioURI(AppState.appSession.AUDIOLOCATION+&quot;agentConnect.vox&quot;)"></audio>
    		</prompt>
		</block>
    	<block>
    		<goto next="#Transfer_ASSIGN" />
    	</block>
	</form>

	<!-- This is the Branching block -->
	<form id="agentCoonectBranch">
		<block>
			<if cond="AppState.appSession.isAgentAvailable=='YES'">
				
				<!-- This is the OutLink -->
				<goto next="#agentTransfer" />
			</if>
			<if cond="AppState.appSession.isAgentAvailable=='NO'">
				
				<!-- This is the OutLink -->
				<goto next="#agentTransferFail" />
			</if>
			<goto next="#agentTransferFail" />
		</block>
	</form>

	<!-- This is the Prompt block -->
	<form id="agentTransferFail">
        
    	<block name="agentTransferFailP0">
  			<prompt bargein="true"  gvp:langexpr="AppState.APP_LANGUAGE">
				<audio expr="getAudioURI(AppState.appSession.AUDIOLOCATION+&quot;agentConnectfail1.vox&quot;)"></audio>
				<foreach item="thePrompt" array="PlayPromptSwitch(AppState.APP_LANGUAGE, AppState.VOXFILEDIR, '1200', 'time')">
					<audio expr="thePrompt" />
				</foreach>
				<audio expr="getAudioURI(AppState.appSession.AUDIOLOCATION+&quot;and.vox&quot;)"></audio>
				<foreach item="thePrompt" array="PlayPromptSwitch(AppState.APP_LANGUAGE, AppState.VOXFILEDIR, '1400', 'time')">
					<audio expr="thePrompt" />
				</foreach>
				<foreach item="thePrompt" array="PlayPromptSwitch(AppState.APP_LANGUAGE, AppState.VOXFILEDIR, 'Monday', 'alphanumeric')">
					<audio expr="thePrompt" />
				</foreach>
				<audio expr="getAudioURI(AppState.appSession.AUDIOLOCATION+&quot;to.vox&quot;)"></audio>
				<audio expr="getAudioURI(AppState.appSession.AUDIOLOCATION+&quot;agentConnectfail2.vox&quot;)"></audio>
    		</prompt>
		</block>
    	<block>
    		<goto next="#Disconnect_ASSIGN" />
    	</block>
	</form>

	<!-- This is the Exit block -->
	<form id="AgentTransferExit">
		<block>
			<log gvp:level="1" label="com.genesyslab.var.ActionEnd" gvp:cond="AppState.EnableReports">agentTransferFlow|UNKNOWN</log>
			<var name="appSession" expr="AppState.appSession"/>
			<var name="AgentTrans" expr="AppState.AgentTrans"/>
			<exit namelist="appSession AgentTrans"/>
  		</block>
	</form>

	<!-- This is the Backend logic block -->
	<form id="CallLog">

		<var name="AppStateString" expr="JSON.stringify(AppState)" />	
		<block>
			<data name="CallLogData" src="../src/callLog.jsp" method="post"  enctype="multipart/form-data"
				namelist="AppStateString" />
        	<if cond="typeof CallLogData.errorMsg == 'undefined'">
        		<assign name="AppState.CallLogResult" expr="CallLogData" />
            	<if cond="typeof AppState.CallLogResult.appSession.ONEFA != 'undefined'">
            		<assign name="AppState.ONEFA" expr="AppState.CallLogResult.appSession.ONEFA" />
            	</if>
            	<if cond="typeof AppState.CallLogResult.appSession.preferLanguage != 'undefined'">
            		<assign name="AppState.preferLanguage" expr="AppState.CallLogResult.appSession.preferLanguage" />
            	</if>
            	<if cond="typeof AppState.CallLogResult.appSession != 'undefined'">
            		<assign name="AppState.appSession" expr="AppState.CallLogResult.appSession" />
            	</if>
				<goto next="#InteractionData1" />
        	<else/>
        		<throw event="error.com.genesyslab.composer.servererror" messageexpr="CallLogData.errorMsg" />
        	</if>
		</block>
	</form>

	<!-- This is the Assign block -->
	<form id="Transfer_ASSIGN">
		<block>
			<script>
				try {
					
					AppState.appSession.TRANSFER_OR_DISCONNECT='T';
					AppState.appSession.DISCONNECTREASON='AGENTTRANSFER';
					} catch (error) {
						System.out.Println(&quot;Exception while send Nomatch Event&quot;+error);
					}
			</script>	
			<goto next="#CallLog" />
		</block>
	</form>

	<!-- This is the Assign block -->
	<form id="Disconnect_ASSIGN">
		<block>
			<script>
				try {
					AppState.appSession.DISCONNECTREASON='SUCCESS';
					} catch (error) {
						System.out.Println(&quot;Exception while send Nomatch Event&quot;+error);
					}
			</script>	
			<goto next="#AgentTransferExit" />
		</block>
	</form>

	<!-- This is the Assign block -->
	<form id="Hangup_ASSIGN">
		<block>
			<script>
				try {
					AppState.appSession.DISCONNECTREASON='HANGUP';
					AppState.nextNode='Disconnect';
					} catch (error) {
						System.out.Println(&quot;Exception while send Nomatch Event&quot;+error);
					}
			</script>	
			<goto next="#Disconnect_ASSIGN" />
		</block>
	</form>

	<!-- This is the Assign block -->
	<form id="error_ASSIGN">
		<block>
			<script>
				try {
					AppState.appSession.DISCONNECTREASON='ERROR';
					AppState.appSession.INVOLUNTARYREASON=='EXCEPTION';
					} catch (error) {
						System.out.Println(&quot;Exception while send &quot;+error);
					}
			</script>	
			<goto next="#Disconnect_ASSIGN" />
		</block>
	</form>

	<!-- This is the Assign block -->
	<form id="allError_ASSIGN">
		<block>
			<script>
				try {
					AppState.appSession.DISCONNECTREASON='AllERROR';
					AppState.appSession.INVOLUNTARYREASON=='EXCEPTION';
					} catch (error) {
						System.out.Println(&quot;Exception while send&quot;+error);
					}
			</script>	
			<goto next="#Disconnect_ASSIGN" />
		</block>
	</form>

	<!-- This is the InteractionDataBlock block -->
	<form id="InteractionData1">
		<property name="com.genesyslab.externalevents.enable" value="false"/>
      	<property name="com.genesyslab.externalevents.queue" value="true"/>
      	
		<block>
			<if cond="AppState.g_CTICCall == 'false'" >
				<script>
					var preferLanguage = AppState.preferLanguage;
					var ANI = AppState.ANI;
					var DNIS = AppState.DNIS;
					var ONEFA = AppState.ONEFA;
				</script>
		    	<gvp:send namelist="preferLanguage ANI DNIS ONEFA" async="false"/>
			<else/>
				<!--  CTIC Case -->
				<script>
					var Action = "AttachData";
					var sub_action = "Add";
					var preferLanguage = AppState.preferLanguage;
					var ANI = AppState.ANI;
					var DNIS = AppState.DNIS;
					var ONEFA = AppState.ONEFA;
				</script>
				<gvp:send namelist="Action sub_action preferLanguage ANI DNIS ONEFA" async="false"/>
    			<gvp:receive maxtime="10s"/>
				<if cond="isCTICResult( application.lastmessage$ ) == 'false'">
    				<throw event="error.com.genesyslab.composer.receiveerror" messageexpr="'The received message has invalid content-type.'" />
    			</if>
    			
    			<var name="InteractionData1Message" expr="application.lastmessage$.content"/>
				<var name="arrUserData" expr="getUserDataArray(InteractionData1Message)"/>
				<var name="InteractionData1Result" expr="utilGetReceiveDataValue('Result', arrUserData)" />
                
				<if cond="InteractionData1Result != 'SUCCESS'">
					<var name="InteractionData1ResultReason" expr="utilGetReceiveDataValue('reason', arrUserData)" />
					<if cond="InteractionData1ResultReason == 'Timeout'">
						<throw event="error.com.genesyslab.composer.operationtimeout" messageexpr="InteractionData1ResultReason" />
					<else/>
						<throw event="error.com.genesyslab.composer.receiveerror" messageexpr="InteractionData1ResultReason" />
					</if>
		 		</if>
			</if>
    		<goto next="#AgentTransfer" />
  		</block>
	</form>

	<!-- This is the Transfer block -->
	<form id="AgentTransfer">
		<property name="inputmodes" value="dtmf" />
	    <transfer name="AgentTransferCall" type="blind" dest="5000"
	    		gvp:analysis="false" gvp:connectwhen="answered" >	
    	</transfer>
	</form>

</vxml>
